{block content}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <h2>Seznam výkupů</h2>
            <div class="col-md-12">
                <a href="{link Vykup:novyVykup}" class="btn btn-primary my-2">Nový výkup</a>


                <div class="row justify-content-center">
                    <div class="col-12">
                        {if $vykupy->count() > 0}
                            <table class="table table-striped">
                                <thead class="table-dark">
                                <tr>
                                    <th>Plomba</th>
                                    <th>Váha</th>
                                    <th>Dodavatel</th>
                                    <th>Prodejní cena</th>
                                    <th></th>

                                </tr>
                                </thead>
                                <tbody>
                                {foreach $vykupy as $vykup}
                                    <tr>
                                        <td>{$vykup->plomba}</td>
                                        <td>{$vykup->vaha} kg</td>
                                        <td>
                                            {if isset($dodavatele[$vykup->dodavatel_id])}
                                                {$dodavatele[$vykup->dodavatel_id]}
                                            {else}
                                                Neznámý dodavatel
                                            {/if}
                                        </td>
                                        <td>{$vykup->prodejniCena} Kč</td>
                                        <td><a href="{link Vykup:editace, vykupId => $vykup->id}" class="btn btn-primary">Editace</a>
                                        </td>
                                    </tr>
                                {/foreach}
                                </tbody>
                            </table>
                        {else}
                            <p>Žádní dodavatelé nebyli nalezeni.</p>
                        {/if}
                    </div>
                </div>
            </div>
        </div>
    </div>
{/block}